function sum(a, b) {
    // Esta es una función llamada 'sum' que toma dos argumentos, 'a' y 'b'.
    // La función devuelve la suma de 'a' y 'b'.
    return a + b;
}

module.exports = sum;
// Se exporta la función 'sum' para que pueda ser utilizada en otros archivos a través de 'require'.
