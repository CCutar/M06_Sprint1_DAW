export const sum = (a: number, b: number) => {
    // Esta es una función llamada 'sum' que toma dos argumentos, 'a' y 'b', ambos de tipo número (number).
    // La función devuelve la suma de 'a' y 'b'.
    return a + b;
};
